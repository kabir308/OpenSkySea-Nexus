# rover_controller

This package is responsible for controlling land-based rovers, specifically those running ArduRover.

It contains launch files and configurations to interface the generic Nexus control messages with the rover.
